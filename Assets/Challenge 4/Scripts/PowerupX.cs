using UnityEngine;

public class PowerupX : MonoBehaviour
{
    public enum PowerupType { Speed, Strength, Slowdown, Magnet, Jump } // Define powerup types
    public PowerupType powerupType; // Assign type in Inspector
}

